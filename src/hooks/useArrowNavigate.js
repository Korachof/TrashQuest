// Allows user to use arrow keys to navigate various processes and modals
import React from 'react';

export default function useArrowNavigate(refs) {
  // TODO: Handle ArrowLeft / ArrowRight to cycle between refs
  // TODO: Handle Enter to trigger .click() on focused element
}
